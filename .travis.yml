language: python
python:
  - "2.7"
# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
# install: PLEASE CHANGE ME
# command to run tests, e.g. python setup.py test
script:  cd src ; python -m unittest discover -p *.py
notifications:
    email:
        recipients:
            - swanand@gmail.com
        on_success: always
        on_failure: always
